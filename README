These files are used for the belizebotanic.org website.  In particular belizebotanic.org/orchid_list

orchid.py: return a list of images based from the gallery based on the tag

orchid_list.py: generate a list of spans and anchors from the orchid list based on whether there are any images tagged with that species
name in the gallery

autotag.py: tag orchid images in gallery with their species names based on their filenames

orchid_param.php: super simple Wordpress plugin to add query parameters to Wordpress permalinks so we can call orchid.py with CGI

